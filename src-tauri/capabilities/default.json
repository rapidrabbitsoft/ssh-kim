{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "app",
  "description": "Default capabilities for the app",
  "local": true,
  "windows": ["main"],
  "permissions": [
    "core:path:default"
  ]
}
